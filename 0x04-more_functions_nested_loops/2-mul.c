#include "main.h"
/**
 * mul - writing a funtion that multiplies two integers
 * @a: first number to multiply
 * @b: second number to multipy
 * Return: result of multiplication of a&b
 */
int mul(int a, int b)
{
return (a * b);
}
